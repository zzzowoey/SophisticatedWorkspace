plugins {
    id "fabric-loom" version "1.2.+" apply false

    id "io.github.juuxel.loom-vineflower" version "1.11.0" apply false // Vineflower, a better decompiler
    id "io.github.p03w.machete" version "1.+" apply false // automatic jar compressing on build
}